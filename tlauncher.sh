sudo apt install -y openjdk-8-jre
sudo apt install -y openjfx

# (If you have Java 9 or 10, you need to install Java 8 and get the launcher to work from it, or completely remove Java 9 or 10.)

# Important! We recommend that you run with root if you run without root privileges, then there are problems with the graphics (gpu).

# Run as follows:
# 1) Navigate to the client folder using the CD command

# 2) Running: 
sudo java -jar TLauncher-2.86.jar

.......................................Debian/Mint.......................................
# 1) Update your system: 
sudo apt -y update 
# , and then 
sudo apt -y upgrade

# 2) Install packets: 
sudo apt install -y default-jdk
